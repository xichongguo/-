name: Update TVBox Config

on:
  workflow_dispatch:
  schedule:
    - cron: "0 23,9 * * *"  # 每天 07:00 和 17:00 UTC+8

permissions:
  contents: write

jobs:
  update-config:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: |
          pip install requests

      - name: Fetch and Update Config
        run: |
          python << 'EOF'
          import requests
          import json
          from datetime import datetime

          # 源配置地址
          CONFIG_URL = "http://cdn.qiaoji8.com/tvbox.json"
          # ✅ 输出文件名：拼音
          OUTPUT_FILE = "xichongyingshi.json"

          # ✅ 直播源：名称改为 xichongtv
          NEW_LIVE = {
              "name": "xichongtv",
              "type": 3,
              "url": "https://raw.githubusercontent.com/xichongguo/live-stream/refs/heads/main/live/current.m3u8",
              "epg": "https://epg.zbds.top/",
              "logo": "",
              "categories": "央视,卫视,体育,电影,综艺,地方"
          }

          try:
              res = requests.get(CONFIG_URL, headers={'User-Agent': 'TVBox-Updater'}, timeout=10)
              res.raise_for_status()
              data = res.json()
              print("✅ Successfully fetched remote config")
          except Exception as e:
              print(f"❌ Failed to fetch config: {e}")
              exit(1)

          # ✅ 强制设置影视站（防止原始配置无 sites）
          data['sites'] = [
              {
                  "key": "xichong",
                  "name": "西充影视",  # 显示名称仍为中文
                  "type": 1,
                  "api": "http://cdn.qiaoji8.com/tvbox.php",
                  "searchable": 1,
                  "quickSearch": 1,
                  "filterable": 1
              }
          ]
          print("✅ Site set to 西充影视")

          # ✅ 清空 spider 防止 TVBox 崩溃
          if 'spider' in data:
              data['spider'] = ""
              print("🔧 Cleared spider field")

          # 确保 lives 存在
          if 'lives' not in data:
              data['lives'] = []
              print("🔧 Initialized lives list")

          # 添加直播源（避免重复）
          if not any(l.get('url') == NEW_LIVE['url'] for l in data['lives']):
              data['lives'].append(NEW_LIVE)
              print("✅ Live source added: xichongtv")  # ✅ 日志也改为 xichongtv

          # 统一设置 EPG
          for live in data.get('lives', []):
              if 'epg' in live:
                  live['epg'] = "https://epg.zbds.top/"

          # ✅ 更新壁纸，显示拼音文件名和时间
          if 'wallpaper' in data:
              data['wallpaper'] = f"xichongyingshi - {datetime.now().strftime('%m-%d %H:%M')}"

          # 保存文件
          try:
              with open(OUTPUT_FILE, 'w', encoding='utf-8') as f:
                  json.dump(data, f, ensure_ascii=False, indent=2)
              print(f"✅ Config saved: {OUTPUT_FILE}")
          except Exception as e:
              print(f"❌ Failed to save file: {e}")
              exit(1)
          EOF

      - name: Commit and Push
        run: |
          git config user.email "action@github.com"
          git config user.name "GitHub Action"
          # ✅ 添加新文件名
          git add xichongyingshi.json
          git commit -m "🔄 Update: xichongyingshi.json [$(date '+%Y%m%d%H%M%S')]" --allow-empty
          git push
